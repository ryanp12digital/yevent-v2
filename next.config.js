
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'picsum.photos', 'yevent.com.br'],
  },
  reactStrictMode: true,
}

module.exports = nextConfig
